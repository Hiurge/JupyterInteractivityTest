# JupyterInteractivityTest

Just checking if widgets are usable from git/NBViewer level as an easy to link dashboard.

TL;DR: not from git, NBViewer (still in development) looks like a good option in future.
